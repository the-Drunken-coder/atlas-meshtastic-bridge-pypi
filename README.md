# atlas-meshtastic-bridge-pypi

Standalone repo for the PyPI package `atlas-meshtastic-bridge`.

- Source of truth lives in the monorepo: `the-Drunken-coder/ATLAS`.
- The `package/` directory is mirrored from the monorepo by a scheduled/manual GitHub Action.
- This repo owns publishing workflows + `CHANGELOG.md`.
